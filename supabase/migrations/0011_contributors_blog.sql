-- ============================================================================
-- contributors.blog_url 추가 — "함께하는 사람들" 블로그 링크
-- ============================================================================
-- 실행: Supabase MCP `apply_migration` (Dashboard 핸드오프 금지). 멱등.
-- ============================================================================

alter table public.contributors
  add column if not exists blog_url text;
