# S1 — 데이터 모델 검증 결과

**일자:** 2026-05-09
**판정:** PASS

## 실행 로그

### 1차 실행
- `pnpm db:migrate:region`: companies.region 25건 백필, idx_companies_region 생성, categories.region_scope 1건 백필
- `pnpm db:migrate:relax-rank`: 테이블 재작성으로 CHECK ≤10 적용

### 2차 실행 (멱등성)
- `pnpm db:migrate:region`: 변경 0건, "이미 존재 — skip"
- `pnpm db:migrate:relax-rank`: "이미 적용됨 — skip"

### 타입체크
- `pnpm exec tsc --noEmit`: 출력 없음 = 에러 0

### 백업
- `data/hegemony.db.bak.1778256996` 생성 (5.3MB, 원본 동일)

## 결론
S1 단계 완료. companies.region (KR=25, INTL=181), categories.region_scope (korea_bio/korea_banks=KR), sector_companies.rank ≤10 적용됨. S2 진행 가능.
