# S3 — 필터 체인 검증 결과

**일자:** 2026-05-09
**판정:** PASS

## 변경
- 22개 파일 수정 (lib 4 + types 1 + API 라우트 8 + hook 8)
- 신규 인터페이스: `ResolvedFilter`, 응답 타입 8개에 `appliedRegion?`
- region 분기 전략 혼용:
  - SQL JOIN: industries, sector-trend, money-flow/industries, money-flow-helpers
  - 메모리 마스크: map, statistics/companies, price-changes
  - helper 옵션: trends 5개 helper

## 검증
- `pnpm exec tsc --noEmit`: PASS
- `pnpm lint`: 우리 수정 파일 에러 0 (사전 존재 에러 44건은 미관련)
- `pnpm build`: PASS

## 하위호환
- region 미지정 → `resolveRegion` 'all' 폴백 → 기존 분기 동일
- queryKey prefix invalidate(`['money-flow']`) 작동
- `appliedRegion?` 옵셔널 — 클라이언트 무시 가능

## 인계 (S4)
- 모든 hook이 `options.region?: RegionFilter` 수용 (default 'all')
- 신규 파일: `hooks/use-region.ts`, `components/region-toggle.tsx`, `components/ui/empty-region-state.tsx`
- 5개 페이지 수정: industry-dashboard, money-flow-page-content, price-changes-page-content, statistics-page, hegemony-map
- regionScope='KR' 카테고리(korea_bio, korea_banks)는 토글 '해외' 시 숨김
