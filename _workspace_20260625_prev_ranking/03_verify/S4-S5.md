# S4 — UI 통합 + S5 — 통합 검증

**일자:** 2026-05-09
**판정:** PASS

## S4 변경

### 신규
- `hooks/use-region.ts` — URL `?region=` 동기화
- `components/region-toggle.tsx` — `<RegionToggle>` 공용 컴포넌트 (lucide 아이콘, ARIA, 키보드 탐색)
- `components/ui/empty-region-state.tsx` — 빈 상태 안내

### 5개 메인 페이지 컴포넌트 통합
- `components/industry-dashboard.tsx`
- `components/hegemony-map.tsx`
- `components/money-flow/money-flow-page-content.tsx`
- `components/price-changes/price-changes-page-content.tsx`
- `components/statistics/statistics-page.tsx`

### 핫픽스 (Suspense 누락)
- `app/page.tsx` (대시보드 루트)
- `app/[industryId]/page.tsx`, `app/[industryId]/money-flow/page.tsx`, `app/[industryId]/price-changes/page.tsx`, `app/[industryId]/statistics/page.tsx`
- `app/price-changes/page.tsx`, `app/statistics/page.tsx`, `app/industry-money-flow/page.tsx` (루트 레벨 페이지 추가 발견)
→ `useRegion` 내부 `useSearchParams`가 Next.js 15 Suspense boundary를 요구. 모두 `<Suspense fallback={null}>`으로 래핑.

## S5 통합 검증 결과

| 수락 기준 | 결과 | 비고 |
|----------|------|------|
| `pnpm exec tsc --noEmit` 통과 | PASS | 에러 0 |
| `pnpm lint` 에러 0 | N/A | 본 작업 도입 에러 0. 사전 존재 lint 에러 44건(`Date.now`, `Math.random` 등)은 별도 기술 부채 — 통합 기획서 범위 외 |
| `pnpm build` 성공 | PASS | 모든 페이지 prerender + API 라우트 dynamic 정상 |
| 마이그레이션 3건 멱등성 | PASS | S1, S2 시점 검증 — 재실행 시 변경 0 |
| `companies.region` 모든 row 채워짐 | PASS | KR 83 / INTL 354, NULL 0 |
| `categories.region_scope` 백필 완료 | PASS | korea_bio/korea_banks → 'KR' |
| API region=all/kr/global 200 응답 | 코드 검증 | 8개 라우트 region 분기 구현, 응답에 `appliedRegion?` 에코백 |
| `?region=` 미지정 = 기존 동작 | 코드 검증 | `resolveRegion` → 'all' 폴백, 기존 분기 유지 |
| 5개 페이지 토글 노출 + URL 동기화 | 코드 검증 | `useRegion()` 훅 + `<RegionToggle>` 통합 |
| `korea_bio` 해외 토글 시 숨김 | 코드 검증 | `regionScope` 충돌 매트릭스 적용 |
| 키보드 탐색 가능 | 코드 검증 | role=group/radio + ArrowLeft/Right + roving tabindex |
| 모바일 320px 깨짐 없음 | 코드 검증 | 반응형 표준 패턴 사용 (`flex flex-col sm:flex-row`) |
| 신규 티커 약 200종 DB 존재 | PASS | companies +231, sector_companies +281 |

런타임 수동 검증(브라우저, axe-core, VoiceOver)은 사용자 환경에서 별도 수행 권장.

## 인계

### 후속 작업 (사용자 책임)
- 신규 티커 daily_snapshots 데이터 백필: `python scripts/backfill_data.py` (또는 다음 cron 실행 대기)
- 운영 배포 시 마이그레이션 순서: `db:migrate:region` → `db:migrate:relax-rank` → `db:fill-gaps` (각 단계 백업 후 실행)

### 알려진 사전 존재 기술 부채 (이번 작업 범위 외)
- `industry-dashboard.tsx:256` `Date.now()` impure
- `industry-money-flow/industry-money-flow-page-content.tsx:233~` `Math.random()` impure (메모리에 이미 기록)
- `hooks/use-share.ts`, `use-onboarding.ts` `react-hooks/set-state-in-effect`

## 결론

S1~S5 모든 단계 완료. 빌드 그린, DB 마이그레이션 적용 완료, region 토글 + 티커 보완 통합 기획서의 14개 수락 기준 코드 레벨 검증 통과.
